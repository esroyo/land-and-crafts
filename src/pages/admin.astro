<script>
    const needle = 'crafts';
    if (window.location.hostname.includes(needle)) {
        // https://landcraftsmap.foo/
        const url = new URL(window.location.href);
        url.hostname = `admin.${url.hostname}`;
        // https://admin.landcraftsmap.foo/
        window.location.href = url.origin;
    }
</script>
